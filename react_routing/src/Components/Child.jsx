import React from 'react'

export const content = {count:0}
function Child(state, action) {
      switch (action.type) {
        case "all":
          return 
        case "com":
          return 
        case "pan":
          return 
      
        default:state
         
      }
}

export default Child